# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=boilerplatedb_admin
DB_USERNAME=boilerplatedb_admin
DB_PASSWORD=admin_password123
DB_NAME=cpa_automation_db
DB_LOG_ENABLED=true
SCHEMA=cpa_automation

# Database URL for Prisma
DATABASE_URL="postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=${SCHEMA}"

# Better Auth Configuration
BETTER_AUTH_SECRET=your-secret-key-change-in-production-use-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# PgAdmin credentials (optional)
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

# Node Environment
NODE_ENV=development
